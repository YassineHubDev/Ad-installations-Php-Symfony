{% extends 'base.html.twig' %}

{% block title %}Votre projet{% endblock %}

{% block content %}

    {{ form_start(magForm) }}
aaaaaaaa

<div id="titre_projet">
    <h2>Faites-nous part de votre projet !</h2>
</div>

<section id="sectionformmag">
    <div id="page_login">
    {{ form_row(magForm.sujet, {'label' : false}) }}
    {{ form_errors(magForm.sujet) }}
    {{ form_widget(magForm.sujet) }}
    
    {{ form_row(magForm.telephone, {'label' : false}) }}
    {{ form_errors(magForm.telephone) }}
    {{ form_widget(magForm.telephone) }}
    
    {{ form_row(magForm.projet, {'label' : false}) }}
    {{ form_errors(magForm.projet) }}
    {{ form_widget(magForm.projet) }}
    
    {{ form_row(magForm.imageFile, {'label' : false}) }}
    {{ form_errors(magForm.imageFile) }}
    {{ form_widget(magForm.imageFile) }}
        
      
    {{ form_widget(magForm) }}
      <button type="submit" class="btn btn-primary" value="Register">Soumettre</button>
    {{ form_end(magForm) }}
    
    </div>
</section>
{% endblock %}


